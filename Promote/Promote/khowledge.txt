1. 只有使用闭包方式创建出的对象才只会创建一次 ，如：

var autoLoginLab: UILabel = {
    let lab = UILabel()
    return lab
}()

而 var autoLoginLab: UILabel {
    let lab = UILabel()
    return lab
}   则每次调用都会重新创建一次


2. iPhone X 尺寸：
#define IS_IPHONE_X (Screen_Height == 812.0f) ? YES : NO

1.导航栏高度 88 非非IPoneX手机为64
2.状态栏高度44 非IPoneX手机为20
3.tabar高度83 非IPhoneX手机为49  如下图所示；

3. setValue:forKey 与value:forKey 必须配对后使用


4. OS X Server搭建Bots持续集成环境
5. 控制器a push或present出控制器b，a都不会被释放，除非rootViewcontroller由1变为1‘。

6. Rxswift： Map 与 FlatMap，merge()， 参考： https://www.cnblogs.com/scott-mr/p/7234769.html#5
